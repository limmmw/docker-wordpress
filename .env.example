# Konfigurasi Database MySQL Eksternal
# Ganti nilai di bawah sesuai dengan konfigurasi database Anda

# Host database (gunakan host.docker.internal jika MySQL di host yang sama)
# Atau gunakan IP server MySQL (contoh: 192.168.1.100:3306)
DB_HOST=host.docker.internal:3306

# Username database
DB_USER=wordpress_user

# Password database
DB_PASSWORD=password_anda

# Nama database
DB_NAME=wordpress_db